set timeout=0
set default=0

# Force pure text mode (NO framebuffer, NO gfx)
set gfxmode=text
set gfxpayload=text
terminal_input console
terminal_output console

menuentry "Kernix (Text Mode)" {
    multiboot /boot/kernel.elf
    boot
}